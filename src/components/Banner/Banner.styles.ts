import { styled } from "@mui/system";
import Box from "@mui/system/Box";

export const BannerWrapper = styled(Box)({
    width: "100%",
    height: "60vh",
    background: "#FF002F",
    display: "flex",
});