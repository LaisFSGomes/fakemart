import { styled } from "@mui/system";
import Box from "@mui/system/Box";

export const BannerInfoWrapper = styled(Box)({
    width: "100%",
    height: "15vh",
    background: "#FF002F",
    display: "flex",
});